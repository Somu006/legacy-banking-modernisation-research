# CLAUDE.md

This repository's AI instructions live in **[`AGENTS.md`](AGENTS.md)**.

Read that file before making any edit. It contains the hard constraints — the
rules that, if broken, silently damage weeks of work:

1. Never edit a generated PDF (edit the markdown, then rebuild)
2. Never remove a `[contested]` or `[ASSUMPTION]` label
3. Never claim the team invented differential testing
4. Never write "proof of correctness"
5. Never invent numbers (ROI, pricing, market size)
6. Never present the demo figures as measured results
7. Never make this repository public (it contains phone numbers)

Reasoning for each: [`docs/DECISIONS.md`](docs/DECISIONS.md).
